FROM r-base:4.4.0

RUN Rscript -e 'install.packages(c("argparser", "tidyverse", "GGally"), repos="https://cloud.r-project.org")'
RUN cd /usr/local && \
    git clone -b pca_plots https://github.com/PRIMED-Ancestry-Inference/PCA_projection.git